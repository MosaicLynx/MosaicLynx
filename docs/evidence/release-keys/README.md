# Release public keys

Store only public Ed25519 SPKI keys here or in `docs/evidence-policy.json`. Record key ID, purpose, creation date, expiry and revocation status. Keep PKCS#8 private signing keys on an offline device; never paste one into an issue, log, CI variable dump, manifest, or this directory.
